[build]
command = "npm run build"
publish = "dist"

# Optional: ensure Node is usable on Netlify
[build.environment]
  NODE_VERSION = "18"

# SPA redirect to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200